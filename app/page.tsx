export default async function Index() {
  // const { getUser } = useSupabaseAuth();
  // const user = await getUser();
  return (
    <div className="w-full flex items-center justify-center">Zalogowano</div>
  );
}
